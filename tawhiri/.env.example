# Tawhiri predictor
# Path where GFS forecast datasets are stored. Can be an external drive.
# Example: /Volumes/mandosanti/tawhiri-data
TAWHIRI_DATA_DIR=/data

# Tawhiri downloader — must match TAWHIRI_DATA_DIR above
DOWNLOADER_DATA_DIR=/data
